/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["images.pexels.com", "fastly.picsum.photos", "cloudinary.com", "next13-cheat-sheet.vercel.app"],
    },
};

module.exports = nextConfig;
